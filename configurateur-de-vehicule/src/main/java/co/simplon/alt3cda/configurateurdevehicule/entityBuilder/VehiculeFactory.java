package co.simplon.alt3cda.configurateurdevehicule.entityBuilder;

import co.simplon.alt3cda.configurateurdevehicule.entity.Vehicle;
import co.simplon.alt3cda.configurateurdevehicule.enumClass.VehiculeEnum;

public class VehiculeFactory {

  // public static IVehicle getVehicule(Vehicle vehicle, VehiculeEnum vehiculeEnum,
  // VehiculeOption vehiculeOption) throws Exception {
  // switch (vehiculeEnum) {

  // case Car:
  // return MotorVehiculeBuilder.getCar(vehicle, vehiculeOption.getDoor(),
  // vehiculeOption.getGearBox());

  // case Moto:
  // return MotorVehiculeBuilder.getMoto(vehicle, vehiculeOption.getCylinder());

  // case Velo:
  // return NonMotorVehiculeBuilder.getVelo(vehicle, vehiculeOption.getChainrings(),
  // vehiculeOption.isAntitheft());

  // default:
  // throw new Exception("this Vehicule don't exist !");
  // }
  // }
}
